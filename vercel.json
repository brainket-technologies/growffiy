{
  "buildCommand": "npx prisma generate && npm run build",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/check-expiry?secret=growffiy_cron_secret",
      "schedule": "0 0 * * *"
    },
    {
      "path": "/api/cron/refresh-tokens?secret=growffiy_cron_secret",
      "schedule": "30 2 * * *"
    }
  ]
}
